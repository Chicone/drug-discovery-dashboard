; ================================
;   Gentle NVT for Martini 3
;   Purpose: stabilize fragile poses
; ================================

define                  =

integrator              = md
dt                      = 0.005      ; ultra-safe: 5 fs
nsteps                  = 200000     ; 1 ns (adjust as needed)

; ----- Temperature control -----
tcoupl                  = v-rescale
tc-grps                 = System
tau-t                   = 2.0
ref-t                   = 310

; ----- Pressure control ------
pcoupl                  = no        ; NVT only

; ----- Position restraints ----
; Make sure your protein has #ifdef POSRES in the .top
; and use "-DPOSRES" for VERY fragile cases.
;define                 = -DPOSRES

; ----- Periodic boundary -----
pbc                     = xyz

; ----- Cutoffs (Martini 3 defaults) -----
coulombtype             = cutoff
rcoulomb                = 1.1
vdw-type                = cutoff
rvdw                    = 1.1

; ----- Neighbour searching -----
cutoff-scheme           = Verlet
nstlist                 = 20
ns_type                 = grid

; ----- Constraints -----
constraints             = none

; ----- COM motion -----
comm-mode               = Linear
comm-grps               = System

; ----- Output control -----
nstxout-compressed      = 1000
nstvout                 = 0
nstenergy               = 500
nstlog                  = 500

; ================================

